let initnalState = {
    navBar: {
        frends: [
            {id: 1, name: 'Dimych'},
            {id: 2, name: 'Andrey'},
            {id: 3, name: 'Sveta'}
        ]
    }
}

const navBarReducer = (state = initnalState, action) => {

    return state;
}

export default navBarReducer